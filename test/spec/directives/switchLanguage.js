'use strict';

describe('Directive: switchLanguage', function () {
    beforeEach(module('cloudifyWidgetPagesApp'));

//    var element;

    it('should make hidden element visible', inject(function (/*$rootScope, $compile*/) {

        // todo : add template html2js for tests.

//    element = angular.element('<div class="switch-language"></div>');
//    element = $compile(element)($rootScope);
//      $rootScope.$digest();
//    expect(typeof($rootScope.toChinese)).toBe('function');
    }));
});
